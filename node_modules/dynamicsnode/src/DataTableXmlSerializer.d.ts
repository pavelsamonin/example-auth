/// <reference types="node" />
import { IDataTableSerializer } from './IDataTableSerializer';
import { DataTable } from './DataTable';
export declare class DataTableXmlSerializer implements IDataTableSerializer {
    readonly extension: string;
    constructor();
    serialize(dataTable: DataTable): Buffer;
    deserialize(xmlContent: Buffer): DataTable;
    private serializeValue(value);
    private parseXmlValue(strValue);
    private parseNumbers(str);
    private parseBooleans(str);
    private parseDates(str);
}
