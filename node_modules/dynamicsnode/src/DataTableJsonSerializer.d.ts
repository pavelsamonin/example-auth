/// <reference types="node" />
import { IDataTableSerializer } from './IDataTableSerializer';
import { DataTable } from './DataTable';
export declare class DataTableJsonSerializer implements IDataTableSerializer {
    readonly extension: string;
    constructor();
    serialize(dataTable: DataTable): Buffer;
    deserialize(data: Buffer): DataTable;
    private static JSONDataReviver(key, str);
    private static parseDates(str);
}
